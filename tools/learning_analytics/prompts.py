"""
学情分析 — DeepSeek 提示词模板
"""
# ════════════════════════════════════════════════════════════
# 班级学情分析
# ════════════════════════════════════════════════════════════

CLASS_ANALYSIS_SYSTEM = """你是一位高校学情分析助手。你的任务是根据班级的出勤和得分数据，给出客观、简洁的分析。
只基于数据说话，不做主观价值观判断。

重要规则：
1. 不同课次的题目数量和总分可能不同，部分课次可能未布置课堂练习（该次课所有学生得分为 0 或空）。
2. 分析得分趋势时，应按"得分率（得分/全班最高分×100%）"做归一化后再比较，而不是直接比较原始分数。
3. 未出课题次的 0 分应视为"无数据"，不计入得分趋势分析。
4. "全班最高分"是指该次课实际得分最高的学生的分数，而非理论满分；得分率反映在该次课内的相对水平。"""

CLASS_ANALYSIS_PROMPT = """请分析以下班级的学情数据，输出 JSON 格式结果。

## 班级基本信息
- 班级名称：{class_name}
- 学生人数：{student_count}
- 课次数量：{session_count}

## 出勤数据（每次课）
{sessions_data}

## 学生个人数据（得分附该次课全班最高分和百分比供参考）
{students_data}

## 要求输出的 JSON 格式
{{
  "summary": "班级整体学情评语（2-3 句话，概括出勤和得分情况）",
  "attendance_rate": "平均出勤率（百分比）",
  "avg_score": "班级平均得分率（各次课得分率的均值，百分比）",
  "trend": "整体趋势描述（进步/退步/平稳，并说明依据。注意：不同课次满分不同，应依据得分率而非原始分数判断趋势）",
  "anomalies": [
    "异常情况列表（如某次课出勤骤降、得分异常等，没有则留空数组）"
  ],
  "attention_students": [
    "需关注学生及其原因（出勤率低 + 得分率低的学生，每条约 20 字）"
  ],
}}

只输出 JSON，不要额外文字。"""

# ════════════════════════════════════════════════════════════
# 个人学情画像
# ════════════════════════════════════════════════════════════

STUDENT_ANALYSIS_SYSTEM = """你是一位高校学情分析助手。你的任务是根据学生的出勤和得分数据，给出客观、简洁的个人学情分析。
只基于数据说话，不做主观价值观判断。

重要规则：
1. 不同课次的题目数量和总分可能不同，部分课次可能未布置课堂练习（该次课所有学生得分为 0 或空）。
2. 评估学生得分时，应参考该次课的全班最高分和得分率来判断相对水平，而非仅看原始分数。
3. 未出课题次的 0 分应视为"无数据"，不纳入得分能力评估。
4. "全班最高分"是该次课实际得分最高的学生的分数，得分率反映在该次课内的相对表现。"""

STUDENT_ANALYSIS_PROMPT = """请分析以下学生的学情数据，输出 JSON 格式结果。

## 学生信息
- 姓名：{name}
- 学号：{student_id}
- 上课次数：{session_count}
- 出勤次数：{attended_count}
- 缺勤次数：{absent_count}
- 请假次数：{leave_count}
- 出勤率：{attendance_rate}

## 各次课得分（附全班最高分/平均分/学生得分率供参考）
{scores}

## 出勤明细（每次课的状态）
{attendance_detail}

## 要求输出的 JSON 格式
{{
  "comment": "个人学情评语（2-3 句话，综合出勤和得分表现，基于得分率而非原始分数）",
  "strength": "优势（出勤好还是得分稳定，或其他）",
  "weakness": "薄弱点（缺勤多还是得分波动大，或其他）",
  "suggestion": "针对性建议（1-2 句话）"
}}

只输出 JSON，不要额外文字。"""
